spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///D:/PDS-1/PDS2022-2/TrabajoFinal/taller2-configData
  security:
    user:
      name: ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}
server:
  port: 9000
